# ───────────────────────── dependencies ─────────────────────────
node_modules/
.pnpm-store/
.pnpm-debug.log*

# ADR-0001 — pnpm is the package manager for this repo.
# Any of these appearing here is a bug, not a preference.
package-lock.json
yarn.lock
bun.lockb
bun.lock
.yarn/
.npm/

# ───────────────────────── build output ─────────────────────────
dist/
build/
out/
*.tsbuildinfo

# ───────────────────────── test + coverage ──────────────────────
coverage/
.nyc_output/
*.lcov
.vitest/
vitest.config.*.timestamp*

# Fixtures that cannot be committed: FIFOs, sockets, device nodes and
# mode-000 directories (Stage 3 adversaries A1/A9). They are created at
# test time by the harness and must never enter the object database.
test/fixtures/**/.generated/

# ───────────────────────── environment ──────────────────────────
.env
.env.*
!.env.example

# ───────────────────────── logs + runtime ───────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
pids/
*.pid
*.seed

# glim writes its debug sink here — AGENTS.md §7 forbids console.log in
# src/, so diagnostics go to a file instead of corrupting the frame.
.glim-debug.log

# ───────────────────────── editors + IDE ────────────────────────
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.sublime-workspace
*~

# ───────────────────────── OS cruft ─────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Desktop.ini

# ───────────────────────── documentation ────────────────────────
# Maintainer's decision, 2026-08-16: the docs are not published.
#
# They remain ON DISK and are still the operating contract — AGENTS.md's read
# order, the stage docs, the ADRs and the retrospectives are all unchanged and
# all still required reading. They are simply not version-controlled from here.
#
# CONSEQUENCE for a future session: a fresh `git clone` of this repo will NOT
# contain docs/. If docs/STATE.md is missing, you are on a clone, not at the
# start of the project — do not "re-initialise" anything. Ask.
docs/

# ───────────────────────── misc ─────────────────────────────────
.cache/
tmp/
temp/
*.tgz
